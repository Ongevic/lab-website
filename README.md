# Lab Website Template

This repository is a generic Jekyll template for research labs, centres, and academic groups.

Replace the placeholder content in `_config.yml`, the collection files under `_people`, `_research`, and `_news`, and the page copy in the root markdown files.

